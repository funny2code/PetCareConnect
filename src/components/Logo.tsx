import { StyleSheet, Text } from 'react-native'
import React from 'react'

const Logo = () => {
  return (
    <Text className="text-3xl font-bold text-blue-500">{`PetCare\nConnect`}</Text>
  )
}

export default Logo

const styles = StyleSheet.create({})